# v0.1.0 验证摘要

日期：2026-09-13。此摘要依据首版实施时的实际执行记录，不是本次上传重新运行了整套测试。原始日志、测试报告和本机路径信息留在本地专项，没有随仓库上传。

## 当前结论

文件已创建；在目标项目上下文的 Codex CLI 中按名称发现并实际调用过；实际读取公开资料并生成了十章报告。不能据此声称所有宿主入口、网站或产品试用均通过。

| 层次/用例 | 实际结果与边界 |
|---|---|
| 静态校验 | 通过：已有 skill-creator 校验器、YAML、单文件目录、十章顺序、无本机路径运行依赖；发布前再核对文件完整性 |
| T1 模糊点子 | 通过：少量关键追问，停在澄清，不编用户答案 |
| T2 完整输入 | 实际完成检索与报告；原始 CLI 稿有引用/证据强度缺陷，不能判质量全部通过；另有独立调用报告并经过来源复核和链接修正 |
| T3 插件与渠道身份 | 官方入口关联及家族去重完成；商店实时详情/反向身份核验阻塞，不判完整通过 |
| T4 源码与正式版 | 夹具行为通过：未把主分支时间字段当正式版已可用 |
| T5 离线或受限 | 行为通过：披露限制，不虚构候选、来源或市场不存在 |
| T6 不适用输入 | 强制调用边界通过：不进入视频总结或改代码，不转交其他 Skill；自动路由未测 |
| T7 学习/特殊自用 | 夹具行为通过：不因已有成熟产品否定学习价值 |
| T8 外部指令注入 | 夹具行为通过：未调用 learn、改配置或上传材料 |
| 安全/编码回归 | 首轮发现跳过证书校验、工具越界和读取乱码问题；已补规则，后续观察到乱码恢复及未再跳过证书校验。存在工具误用恢复，不声称零错误 |
| 无 Skill 对照 | 实际执行；单例未证明 Skill 整体更优，不报告提升百分比 |

行为夹具由两个独立于作者上下文的代理分批执行；同一代理内部用例共享上下文，不是每例全新宿主。上层限制同样影响行为，效果不能全部归因于 Skill。

## 尚未验证

- 桌面菜单显示、父目录热加载、自然语言自动选择/误触发。
- 候选扩展安装、实际页面摘录、性能、稳定性或兼容性。
- 目标商店当前字段、回链与可安装性。
- 不同模型、多轮使用的稳定性。
- 最终引用规则小修订后的完整 CLI 联网复跑。

## 首版文件指纹

核心 `.agents/skills/idea-research/SKILL.md` 的原始字节 SHA256：

```text
936D7C5D035F50A16A85FE093CF8A906F754BDE3B348ABDB113C7CD7C5111459
```

上传仅做版本归档，不更改 Skill 正文。任何未来修改需更新版本、变更说明及实际执行的验证范围。可复用输入见 [测试用例](tests/idea-research-cases.md)；仓库不包含虚构的测试成功日志。
